[project]
name = "web-xray-dashboard-backend"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
  "fastapi",
  "uvicorn",
  "sqlalchemy",
  "aiosqlite",
  "pydantic-settings",
]

[tool.pytest.ini_options]
pythonpath = ["."]
