[project]
name = "webxray-agent"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
  "fastapi",
  "uvicorn",
  "pydantic-settings",
  "cryptography",
]

[tool.pytest.ini_options]
pythonpath = ["."]
